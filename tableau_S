I used REGEXP_REPLACE and TRIM in Tableau to remove special characters and normalize customer names with single spaces.
To ensure only single spaces, use this final version 

TRIM(
    REGEXP_REPLACE(
        REGEXP_REPLACE(
            [Customer Name],
            "[^A-Za-z ]",
            ""
        ),
        "\s+",
        " "
    )
)
